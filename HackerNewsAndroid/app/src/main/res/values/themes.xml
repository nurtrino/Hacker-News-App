<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this only needs to keep the system bars and
         the pre-Compose window background from flashing the wrong colour. -->
    <style name="Theme.HackerNews" parent="android:Theme.Material.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
